# Chart left grid line

Replaced the chart background's repeating vertical grid with explicitly positioned guide lines. This preserves the existing spacing and right boundary while eliminating the repeated tile that leaked an unintended line onto the left edge.
