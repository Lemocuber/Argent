# Deletion control layout

Increased the separation between channel type configuration and deletion confirmation. Rebuilt the destructive control with a uniform thin red chamfered outline and centered its divider so the full-archive and dated-closure actions occupy equal halves.
