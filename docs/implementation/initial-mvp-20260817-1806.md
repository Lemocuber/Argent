# Initial MVP — 2026/08/17

Built the first complete Argent experience: emoji-sequence identity with automatic return entry, chart-first mobile navigation, channel creation and editing, signed two-decimal balance snapshots, notes, historical dates, derived changes, and cash/total/net-worth chart modes.

Added the Express and SQLite backend, production static serving, a daily backup check, PWA assets, local Material SVGs, and the minimalist monoline visual system. Verified the production build, exercised the API against a temporary SQLite database, and inspected the login, chart, and report screens in a mobile-sized headless browser.
