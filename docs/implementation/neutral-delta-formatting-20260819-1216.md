# Neutral delta formatting

Standardized signed-change formatting across channel rows, the entry editor, and chart details. Positive and negative changes retain their signs, while every displayed zero change now uses `±0`.
