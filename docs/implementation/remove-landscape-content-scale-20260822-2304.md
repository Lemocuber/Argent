# Remove landscape content scale

Removed the extra 1.5× interface scaling inside the centered landscape phone frame. The frame keeps its responsive size, border, and subtle corner radius while its contents now render at the browser's natural scale.
